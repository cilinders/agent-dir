0. git
	- git project: branch development
		* .gitignore
	- GitHub remote
	- ssh-agent script
		* usage: sshScript.sh
			^ git: starts ssh-agent en add ssh pub > allows pushing to remote repo
		* conf:
			^ 
	- git-script
		* usage: gitScript.sh
			^ lazy: commits and pushes changes lazyly for eod
1. VM container
	- Docker: iffy build
		* Docker engine bash script
			` usage: scriptDocker.sh
				^ start: starts docker engine
				^ stop: stops docker engine
				^ build: builds image
				^ destroy: destroys container
				^ destroyclean: destroys image and container
				^ run: runs docker container
				^ log: dumps logfile from container
			` conf:
				^ project docker path
				^ project script path
				^ docker image name
				^ docker container name
		* Docker image: alpine
			` -v 3.22.1
		* Dockerfile:
			` wkdir /agent-dir
			` logs
				^ buildtime build<time>.log
		* Apparmor, & rootless mode
			` docker run --security-opt apparmor=docker-default
			` https://docs.docker.com/engine/security/apparmor/
2. llm local
	- model
		* XdeepseekX V ollama3.1 ? 
	- local agency
		* command line excess
		* action steps
			` action path
			` action set
		* safeguards
			` stay inside container*** ***breakout to internet oke
			` protect local container
			` guard earlier quarentined output
		* context memory
		* agent ruleset
			` taskset
			` config rules
	- allow searching web

---
in: what
asks steps
asks start step
step
done?
repeat
---
