#!/bin/bash

SYSTEM="You are a validator that validates the tree string, which should be valid and only contain files and directories, in software development commits, respond only with true or false. Explain why this is the case for the given commit."
TEMPERATURE=0.1
