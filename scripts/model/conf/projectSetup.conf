#!/bin/bash
SYSTEM='You are a helpful assistant. Output the correct directory name for this project. Only return a string of text with the directory name without additional commentary.'
TEMPERATURE=0.1
